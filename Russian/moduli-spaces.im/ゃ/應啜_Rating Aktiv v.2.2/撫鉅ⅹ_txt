/**
* Название: Рейтинг активности
* Автор: ArsWay (TMS)
* Текущая версия: 2.2 (Для DCMS 6.6.*, 6.7.*)
* Автор: ArsWay (TMS)
Перепродажа скрипта, выкладывание в общий доступ ЗАПРЕЩЕНЫ!
P.S.: Уважайте чужой труд!
*/

1) Распоковать архив в корень сайта

2). Заходим в админку и делаем запрос в базу:
ALTER TABLE `user` ADD `akt_rating` VARCHAR( 8 ) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL;
ALTER TABLE `user` ADD `time_click` INT( 11 ) NOT NULL;
ALTER TABLE `user` ADD `time_click_r` INT( 11 ) NOT NULL;
ALTER TABLE `user` ADD `aktiv_minus` ENUM( '0', '1' ) NULL DEFAULT NULL;
 
3). Скрипт готов к работе
