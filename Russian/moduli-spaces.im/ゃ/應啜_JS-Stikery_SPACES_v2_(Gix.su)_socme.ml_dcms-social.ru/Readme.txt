Скрипт -> Стикеры v2Цена -> 300 рубАвтор -> IslamfonУстановка: 1) Распаковать архив. Заменяет всего один файл output_text.php. Если он у вас модифицированный, то напишите мне об этом. Я переделаю.2) Если у вас есть еще web темы оформления, помимо стандартной, то нужно файл altername_post_form.php из style/themes/web добавить во все папки web оформлений из папки themes.3) Залить таблицы из файла table.sqlТеперь придется вручную устанавливать куда нам нужно:1) Заходим в файл, находим строку формы:Примерно: echo '<form method="post" name="message" action="?" >';Перед этой строкой вставляем следующее:echo'<script language="javascript" type="text/javascript" src="/sys/stickers/js.js"></script><link rel="stylesheet" href="/sys/stickers/style.css" type="text/css" />';2) Далее находим строку textarea:Примерно: <textarea name="msg">Вставляем туда id="textarea" И получаем строку: <textarea id="textarea" name="msg">3) Затем, после строки <textarea id="textarea" name="msg"> вставляем следующее:echo '<div id="smiles_place"></div><div id="block_for_stickers"></div>';4) Далее после строки <input type="submit"> пишем:echo "<a href='#stick' onclick='return addStickerButton();' id='addStickerButton2''>&nbsp;<span class='stick'></span></a>";5) Теперь находим строки:$mat = antimat($msg);if ($mat)$err[] = 'В тексте сообщения обнаружен мат: ' . $mat;	Сразу после них пишем:if (mysql_result(mysql_query("SELECT COUNT(*) FROM `stickers`  WHERE `id_user` = '$user[id]' AND `time` > '$time' LIMIT 1"),0) == 0){$st = mysql_query("SELECT * FROM `stickers_array` ");while ($str = mysql_fetch_array($st)){if (strstr($msg, $str['text'])) $err[] = 'Подключите услугу: <a href="/user/money/pay.php">Стикеры</a>';}}$msg у некоторых файлов может отличаться. Узнать это вы можете здесь $mat = antimat($msg);Если внутри скобок у переменной другое название, то необходимо заменить переменную и вот здесь if (strstr($msg,=============================================	Если вы выполнили все 5 условий правильно, то рядом с кнопкой появится иконка для открытия панели стикеров.Если у вас будет что-то не получатся, то пишем мне в личку: http://gix.su/user/islamfon